<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinsa.palletpang.domain.user.UserMapper">
    <select id="selectUserName" resultType="String">
        select 'deneb'
    </select>

    <select id="selectUser" resultType="com.sinsa.palletpang.domain.user.User">
        select 'deneb' as name
    </select>
</mapper>